package org.dragon.skill.enums;

/**
 * Skill 可见性枚举。
 * PUBLIC：公开，在 Skill 广场中对所有人可见，任何 workspace 均可圈选。
 * PRIVATE：私有，仅创建人可见，不出现在广场中。
 *
 * @since 1.0
 */
public enum SkillVisibility {
    PUBLIC,
    PRIVATE
}